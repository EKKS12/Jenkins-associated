pipeline{
    agent any
    stages{
        stage("build"){
            steps{
                echo "Building..."
            }
            post{
                success{
                    emailext to: "ekks28@gmail.com",
                    subject: "Build status Email",
                    body: "Congratulations!! The linking between Jenkins Pipeline and Github was completed. The use Jenkins Git plugin and Repository URL menu has successfully joined Jenkins(Github pipeline) and Github(Jenkins-associated). Microsoft Visual Studio did not find any issues in final version.",
                    attachLog:true
                }
            }
        }
    }
}
